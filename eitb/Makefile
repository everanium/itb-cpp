# Delegates to the binding's top-level Makefile so the compiler flags
# and libitb wiring stay in one place. `make` here produces ./eitb.

all:
	$(MAKE) -C .. eitb

clean:
	rm -f eitb

.PHONY: all clean
